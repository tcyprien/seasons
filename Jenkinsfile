pipeline {
  agent any
  stages {
    stage ("build") {
      steps {
        echo "build step"
      }    
    }
  stage ("test") {
      steps {
        echo "test"
      }    
    }
  stage ("deploy") {
      steps {
        echo "deploy step"
      }    
    }
  stage ("staging") {
      steps {
        echo "staging step"
      }    
    }
  }
}
